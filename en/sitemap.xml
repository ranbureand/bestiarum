---
type: sitemap
layout: null
navigation: false

title: Sitemap

sitemap:
  exclude: 'yes'
---

<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">

  {%- assign pages = site.pages | where: 'language', 'en' | where: 'published', true %}

  {%- for page in pages %}
    {%- unless page.sitemap.exclude == 'yes' or page.name == 'feed.xml' %}
    <url>
      <loc>{{ site.url }}{{ page.url | remove: 'index.html' }}</loc>
      {%- if page.sitemap.lastmod %}
        <lastmod>{{ page.sitemap.lastmod | date: '%Y-%m-%d' }}</lastmod>
      {%- elsif page.date %}
        <lastmod>{{ page.date | date_to_xmlschema }}</lastmod>
      {%- else %}
        <lastmod>{{ site.time | date_to_xmlschema }}</lastmod>
      {%- endif %}
      {%- if page.sitemap.changefreq %}
        <changefreq>{{ page.sitemap.changefreq }}</changefreq>
      {%- else %}
        <changefreq>monthly</changefreq>
      {%- endif %}
      {%- if page.sitemap.priority %}
        <priority>{{ page.sitemap.priority }}</priority>
      {%- else %}
        <priority>0.3</priority>
      {%- endif %}
    </url>
    {%- endunless %}
  {%- endfor %}

  {%- assign seminars = site.seminars | sort: 'date' | where: 'language', 'en' | where: 'published', 'true' %}

  {%- for seminar in seminars reversed %}
    <url>
      <loc>{{ site.url }}{{ seminar.url }}</loc>
      {%- if seminar.sitemap.lastmod %}
        <lastmod>{{ post.sitemap.lastmod | date: '%Y-%m-%d' }}</lastmod>
      {%- elsif seminar.date %}
        <lastmod>{{ seminar.date | date_to_xmlschema }}</lastmod>
      {%- else %}
        <lastmod>{{ seminar.time | date_to_xmlschema }}</lastmod>
      {%- endif %}
      {%- if seminar.sitemap.changefreq %}
        <changefreq>{{ seminar.sitemap.changefreq }}</changefreq>
      {%- else %}
        <changefreq>monthly</changefreq>
      {%- endif %}
      {%- if seminar.sitemap.priority %}
        <priority>{{ seminar.sitemap.priority }}</priority>
      {%- else %}
        <priority>0.5</priority>
      {%- endif %}
    </url>
  {%- endfor %}

  {%- assign posts = site.posts | sort: 'date' | where: 'language', 'en' | where: 'published', 'true' %}

  {%- for post in posts reversed %}
    <url>
      <loc>{{ site.url }}{{ post.url }}</loc>
      {%- if post.sitemap.lastmod %}
        <lastmod>{{ post.sitemap.lastmod | date: '%Y-%m-%d' }}</lastmod>
      {%- elsif post.date %}
        <lastmod>{{ post.date | date_to_xmlschema }}</lastmod>
      {%- else %}
        <lastmod>{{ site.time | date_to_xmlschema }}</lastmod>
      {%- endif %}
      {%- if post.sitemap.changefreq %}
        <changefreq>{{ post.sitemap.changefreq }}</changefreq>
      {%- else %}
        <changefreq>monthly</changefreq>
      {%- endif %}
      {%- if post.sitemap.priority %}
        <priority>{{ post.sitemap.priority }}</priority>
      {%- else %}
        <priority>0.5</priority>
      {%- endif %}
    </url>
  {%- endfor %}

</urlset>